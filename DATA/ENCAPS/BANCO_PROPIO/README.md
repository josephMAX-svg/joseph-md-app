# 🏦 BANCO_PROPIO — depósito único de preguntas ENCAPS con clave verificada (mantenimiento 2027-I → intensiva)

> Todo lo que Joseph resuelve en la hora ENCAPS (16:15-17:15) y en los viernes sale de aquí. Cada ítem cita **contra qué se verificó su clave** (gate §3-bis de `PROTOCOLO_GENERACION_PREGUNTAS.md`). **LISTA NEGRA:** ningún fichero de este depósito deriva del examen 2026-II hasta el pre-test del 5-feb-2027 (`PRETEST_2026-II.md`); el generador rechaza cualquier ítem cuyo `verificado_contra` / `molde` / `fuente` mencione `2026-2` o `2026-II`.

## 1) Ficheros

| Fichero | Qué es | Ítems | Entra al pool del runner |
|---|---|---|---|
| `_examenes_reales_2024-2A_2026-1.json` | Los 6 exámenes reales (2024-2A/2B · 2025-1A/1B · 2025-2 · 2026-1) con enunciado, 4 opciones y **clave oficial** extraída de los resaltados del PDF (2026-1 sin clave). A/B son el mismo examen barajado. | 600 (400 únicos con clave) | no (referencia; `--sim100 <proceso>` lo usa como simulacro 100Q) |
| `_etiquetas_examenes_reales_v3.json` | Etiquetado por **código v3 + sub_eje + formato** de los 300 ítems con clave (2024-2A · 2025-1A · 2025-2), hecho enunciado por enunciado el 05-sep-2026. Línea base real por código. | 300 | no (los ítems etiquetados viven en `set_*_1.json` y `set_reales_otros_1.json`) |
| `_inventario_banco_por_codigo.json` | Oferta vs demanda por código v3 (§4). Se regenera con `--inventario`. | — | no |
| `banco_items_v1.json` | 50 ítems espejo (moldes 2024-2A→2026-1, 44 con clave oficial) diseñados para los 2 primeros mini-sims (`reservado_para` 18-sep / 25-sep desde el corrimiento v5.10; antes 11-sep / 18-sep). | 50 | sí |
| `set_<codigo>_1.json` × 8 | **Stock real de las 2 primeras semanas de la rotación** (sem A: II-3, I-3, V-2, III-5 · sem B: II-5, I-4, IV-1+IV-2, II-4). 25Q por código = ítems **reales** re-etiquetados (clave oficial) + ítems **nuevos** cuya clave cita el texto extraído de los compendios locales (López 2026-I / Theomed 2026, pdftotext 05-sep) y la norma. Reparto: I-3 25 reales · V-2 25 reales · IV-1+IV-2 21+4 · I-4 17+8 · III-5 15+10 · II-5 8+17 · II-4 6+19 · II-3 5+20. | 200 | sí |
| `set_reales_otros_1.json` | Resto de ítems reales etiquetados (cola larga, rebotes II-1/II-11/II-8, III-8, V-MED, IV-6+IV-7, 11 V-2 sobrantes). Alimenta los **secundarios** de cola larga y los mini-sims. | 178 | sí |
| `set_cola_larga_1.json` | Ítems nuevos para códigos de cola larga con < 4 reales (hoy V-6 telesalud ×6, Compendio López Gestión 2026-I). | 6 | sí |
| `minisim_<viernes>.json/.html` | Mini-simulacro de viernes (25Q, receta fija) ya armado + runner HTML. | 25 c/u | salida (sus ids quedan "usados") |
| `banco_<fecha>.json/.html` | Banco del día lun-jue (16-20Q del código/sub-eje + 4-5Q del secundario), corrección inmediata. | 20-25 c/u | salida |
| `eval_<fecha>.json/.html` | Eval anclada 16:15 (5Q = 3 cifras + 2 viñetas del código de ayer; lunes = fallos previos). | 5 c/u | salida |
| `pretest_arranque_2026-09-14.json/.html` · `pretest_arranque_2026-09-15.json/.html` | **Pre-test de arranque** (línea base ciega por crítico): 40Q = 5Q × 8 críticos v3 en dos partes de 20Q (parte 1: II-3 · I-3 · V-2 · III-5 · parte 2: II-5 · I-4 · IV-1+IV-2 · II-4), ítems **reales** con clave oficial 2024-2A/2025-1A/2025-2 (36/40; II-3 4R+1N, III-5 2R+3N: sus reales restantes están en `eval_2026-09-15` y `banco_2026-09-17`), modo examen 72 s/Q, confianza obligatoria. **Sustituyen el contenido** de `banco_2026-09-14` y `banco_2026-09-15` (marcados `_meta.sustituido_por`; su HTML avisa y no arranca). Ronda `pretest` con `preguntas[]` → `--registrar … --append`. | 20 + 20 | salida (sus ids quedan "usados"; los de los dos bancos sustituidos vuelven al pool) |
| `pretest_2026-II.html` · `sim100_<proceso>_<fecha>.html` | Pre-test del 5-feb-2027 (generar el jue 4-feb) y simulacros 100Q de la intensiva. | 100 | salida |

## 2) Esquema de ítem (el de `banco_items_v1.json`, ampliado)

```json
{ "id": "R-2025-2-Q22 | N-II3-001 | BP-0001", "codigo": "I-3 | V-2 | IV-1+IV-2 | V-MED …", "area": "I|II|III|IV|V",
  "formato": "viñeta | directa | cifra | viñeta+cifra", "critico": true, "sub_eje": "clave de SUB_EJES en _encaps_ciclo_v3.js", "subangulo": "label",
  "fallo_previo": "clave del sub-ángulo fallado en _registro_resoluciones.json que este ítem re-testea (o null)", "reservado_para": "viernes YYYY-MM-DD o null",
  "dif": "media|alta|real", "origen": "examen_real | nuevo_05sep2026 | espejo",
  "enunciado": "…", "opciones": { "A": "…", "B": "…", "C": "…", "D": "…" }, "clave": "B", "respuesta": "texto de la clave", "explicacion": "…|null",
  "fuente": "de dónde sale el contenido", "verificado_contra": "CLAVE OFICIAL <proceso> Q<n> = … | Compendio <x> (texto extraído): '…' | norma con número y año",
  "molde": "<proceso> Q<n> | sin molde" }
```
Prefijos de id: `R-` real · `N-` nuevo · `BP-` espejo v1. 4 opciones A-D (el examen real tiene 4). Un fichero = `{ _meta, items[] }`; cualquier `*.json` que no empiece por `_` ni sea salida del runner entra al pool.

## 3) Cómo se consumen (todo con `node DATA/_scripts/gen_encaps_minisim.js`)

| Cuándo | Comando | Qué arma | Reglas |
|---|---|---|---|
| Lun-jue 16:30 **banco del día** | `--banco 2026-09-14` | lee la fila `banqueo1h` de `_encaps_mantenimiento_2027.sql` (código, `extra.sub_eje`, secundario) → 16-20Q del código ciñéndose al **sub-eje del día** (luego otros sub-ejes del código) + 4-5Q del secundario de cola larga; corrección **inmediata** al marcar respuesta + confianza; ≥40 % recall directo | avisa si el sub-eje tiene < 8Q o el secundario < 4Q |
| Mar-jue 16:15 **eval anclada** | `--eval 2026-09-15` | 5Q del código de AYER: 3 cifras + 2 viñetas (prefiere el sub-eje de ayer), solución al final; lunes **y D1 del régimen** (sin sesión anterior en el SQL): 5Q de fallos previos, críticos primero | el viernes no lleva eval (el mini-sim ocupa las 16:15) |
| Viernes 16:15 **mini-sim** | `2026-09-25` | 25Q receta fija (8 II · 7 I · 5 V · 3 III · 2 IV, 50/50 viñeta-directa, ≥10 críticos, ≥5 fallos previos, 5-6Q de los 2 códigos de cola larga del viernes), 72 s/Q | escribe `minisim_<fecha>.json/.html`; exige 25Q |
| **Lun 14 + mar 15-sep: pre-test de arranque** | `--pretest-arranque [lunes]` (por defecto el D1 del SQL) | 2 × 20Q = 5Q × 8 críticos v3, ítems reales con clave oficial 2024-2A→2025-2 (nunca el 2026-II), un sub-eje distinto por ítem mientras haya, orden barajado, modo examen 72 s/Q, solución al final, confianza obligatoria; marca `banco_<lunes>` y `banco_<martes>` como sustituidos (JSON `_meta.sustituido_por` + aviso en el HTML) | regla de ids: nunca un id de `eval_`/`minisim_`/otros `banco_` vigentes; los ids que SOLO estaban en los dos bancos sustituidos vuelven a estar disponibles y se prefieren real libre → real del banco sustituido → nuevo. `--semana`/`--banco` no regeneran un día sustituido (`--forzar` para ignorarlo). Detalle en `_meta.seleccion` |
| Toda una semana de golpe | `--semana 2026-09-14` (siempre el LUNES de la semana, aunque no tenga sesión) | banco lun-jue + eval mar-jue + mini-sim del viernes (si no existe); los días sin fila en el SQL se saltan diciendo POR QUÉ, leído del propio SQL: «anterior al D1 del plan (2026-09-14)», «posterior al cierre del plan (2027-01-29)» o «feriado del régimen» (25-dic · 31-dic · 1-ene) | pre-generar el viernes anterior (regla: ≥5 sesiones de stock por delante); es determinista: las salidas `banco_`/`eval_` de esa misma semana que ya existan se sobrescriben y sus ids viejos no cuentan como «usados» hasta que cada una se regenera |
| Tras un corrimiento del régimen | `--realinear [--dry]` | mueve cada `minisim_<viernes>` ya armado cuya cola larga (`_meta.cola_larga.codigos`) ya no coincide con la fila `mini_sim` de su fecha al primer viernes ≥ su fecha con la MISMA cola larga y sin mini-sim: ítems intactos, `_meta.realineado` con el motivo y los `reservado_para` del pool re-fechados; si no encaja en ningún viernes lo borra (fecha sin sesión) o lo regenera (sigue siendo viernes de mini-sim) | correrlo ANTES de `--semana` del D1 nuevo; el 12-sep movió 11→18, 18→25 y 25→2-oct sin borrar ni regenerar ninguno |
| Fin de sesión | `--registrar <export.json> [--append]` | guarda la ronda en `TRACKING_ERRORES/RONDAS/` y da la línea para `gen_encaps_semana.js --cerrar` | append-only |
| Inventario | `--inventario` | `_inventario_banco_por_codigo.json` | — |
| Intensiva | `--pretest` (jue 4-feb) · `--sim100 2025-2|2025-1A|2024-2A|propio <fecha>` | 100Q / 72 s | `propio` exige ≥100 ítems no usados |

**Fallos previos (≥5Q del mini-sim):** el runner solo puede cumplirlos si el pool tiene ítems con `fallo_previo` = clave de un sub-ángulo fallado en `_registro_resoluciones.json` (o del código en estado "débil" en `resumen_por_subtema`). Hoy solo I-3 tiene fallos registrados y sus ítems etiquetados ya se consumieron (semana 1): al cerrar cada semana con `gen_encaps_semana.js`, los sets nuevos deben traer `fallo_previo` con las claves recién falladas y el mini-sim del viernes siguiente se regenera (`node … <viernes>`; sobrescribe su propio fichero sin tocar los demás). Mientras tanto el runner avisa "fallos previos: n/5" y rellena con críticos.

**Stock pre-generado hoy (12-sep-2026, re-fecha a D1 = lun 14-sep · régimen v5.10):** `banco_2026-09-14` (**D1** · II-3 · esquema_intervalos + secundario II-2, 22Q; el lunes no lleva eval) · `eval_2026-09-15` + `banco_2026-09-15` (I-3 · tipos_vigilancia + I-10, 25Q) · `eval_2026-09-16` + `banco_2026-09-16` (V-2 · planeamiento + V-6, 25Q) · `eval_2026-09-17` + `banco_2026-09-17` (III-5 · pertinencia_barreras + II-6, 25Q) · `minisim_2026-09-18` (II-2 + I-10) · `minisim_2026-09-25` (V-6 + II-6) · `minisim_2026-10-02` (II-10 + I-5+I-6). Al correr el régimen del vie 11 al lun 14, el **11-sep dejó de tener sesión** y la cola larga de los viernes rotó una semana entera, así que los tres mini-sims ya armados se **movieron sin regenerar** con `--realinear` (11-sep → 18-sep · 18-sep → 25-sep · 25-sep → 2-oct: los mismos 25 ítems cada uno, en el mismo orden, con `_meta.realineado` explicando el movimiento; los 50 `reservado_para` de `banco_items_v1.json` quedaron re-fechados 11→18 y 18→25). No se borró ni se regeneró ningún mini-sim porque cada cola larga encontró su viernes nuevo, y no desapareció ningún `banco_`/`eval_`: las filas `banqueo1h` del 14 al 17 son las mismas que en la v5.9 (solo baja 1 su D#: el 14-sep es ahora D1), así que `--semana 2026-09-14` las regeneró con **los mismos ids en el mismo orden** que la corrida del 10-sep (solo cambian `_meta.generado` y `_meta.fila_sql.dia`). **10 ficheros de salida · 187 ítems · 0 ids repetidos · 0 ítems derivados del 2026-II** (verificado el 12-sep con node: ningún `fuente`/`molde`/`verificado_contra` cita ese proceso; tres ítems del pool —BP-0010, BP-0012, BP-0017, moldes 2024-2A/2025-1A con clave oficial— solo lo mencionan como comentario dentro de su `explicacion`). Del pool de 434 ítems quedan **247 disponibles** tras pre-generar esta semana y los 3 mini-sims (`_inventario_banco_por_codigo.json`, `_meta.totales`): hay que reponer antes de la semana 3. **`--semana` es determinista por construcción** desde el 12-sep (las salidas `banco_`/`eval_` de la propia semana que va a sobrescribir no cuentan como «usadas» hasta que cada una se regenera, así que da lo mismo que existan o no), y quedan en **0Q disponibles** los códigos **II-3 · I-3 · II-2 · II-6** (el secundario del 14-sep entró con 2Q de las 4-5 de la receta) frente a **V-2 6Q · III-5 5Q · II-5 25Q · I-4 21Q**, así que la semana del **lun 21-sep** ya necesita `set_II-3_2.json`, `set_I-3_2.json` y cola larga II-2/II-6 antes de pre-generarse.

**Actualización 12-sep (pre-test de arranque, gaps_v3b_encaps punto 8):** `node DATA/_scripts/gen_encaps_minisim.js --pretest-arranque` generó `pretest_arranque_2026-09-14` (parte 1: II-3 · I-3 · V-2 · III-5) y `pretest_arranque_2026-09-15` (parte 2: II-5 · I-4 · IV-1+IV-2 · II-4): **40 ids únicos, 36 reales con clave oficial** (2024-2A 14 · 2025-1A 10 · 2025-2 12), 0 solapes con `eval_2026-09-15/16/17`, `banco_2026-09-16/17` y los 3 mini-sims, 0 ítems derivados del 2026-II (verificado con node). Los dos primeros bancos del día quedan **sustituidos en contenido** (`banco_2026-09-14.json`/`-15.json` llevan `_meta.sustituido_por` y su HTML avisa en rojo y no arranca): el lunes 14 y el martes 15 se resuelve el pre-test a las 16:30 (el horario y las filas de `study_schedule` no cambian; `eval_2026-09-15` sigue siendo II-3 y `eval_2026-09-16` I-3, ambos cubiertos por la parte 1). Consecuencia en el stock: II-3 e I-3 pasan de 0 a **15 disponibles** cada uno (los ítems no tomados de los bancos sustituidos vuelven al pool), V-2 queda con **1** y III-5 con **0** (§4). La eval del lunes 21 y los ≥5Q de fallos previos del mini-sim del 25-sep deben construirse desde los fallos del pre-test (exportar cada parte y apendar con `--registrar <export.json> --append`).

**Un ítem se usa una sola vez:** los ids presentes en cualquier `minisim_*.json`, `banco_*.json`, `eval_*.json` o `pretest_arranque_*.json` quedan excluidos de los sets siguientes (sin importar la fecha, porque los sets se pre-generan por adelantado). **Única excepción:** una salida marcada `_meta.sustituido_por` (hoy `banco_2026-09-14` y `banco_2026-09-15`, sustituidos por el pre-test de arranque) NO consume ids: esa sesión no se resuelve, así que sus ítems que el pre-test no tomó vuelven al pool (por eso II-3 e I-3 pasan de 0 a 15 disponibles). Para reponer stock se añaden ficheros `set_<codigo>_<n>.json` (nunca se editan ids ya consumidos). `--dry` en cualquier modo solo informa.

Cada runner HTML abre en el navegador sin red: reloj, confianza 1-3 obligatoria, corrección por código/sub-eje/formato (% bruto y **% CIEGO = seguras/total**), clasificación del error (CONCEPTO · OLVIDO · CRONOLOGÍA · CCSN · CONTEXTO · CAMBIO · TIEMPO · LECTURA) y export JSON en el esquema de ronda v3 + línea de cierre. Estado en `localStorage` (`jmd-encaps-runner-<id>`).

## 4) Stock vs demanda (inventario regenerado el 12-sep-2026 contra el SQL de D1 = lun 14-sep)

Siembra vigente (**D1 = lun 14-sep-2026 · 97 días · cierre vie 29-ene-2027**, régimen v5.10, backup `study_schedule_bk_0912`): **79 sesiones `banqueo1h` + 18 mini-sims de viernes**, con **I-3 10 · V-2 10 · II-8 4 · el resto 5 sesiones** cada uno (I-4 · II-1 · II-3 · II-4 · II-5 · II-11 · III-5 · III-8 · IV-1+IV-2 · IV-6+IV-7 · V-MED) y 79 slots de secundario de cola larga + 36 slots de cola larga en los viernes. Demanda sembrada en 97 días = **1 880Q** (leída de `_inventario_banco_por_codigo.json`, `_meta.totales.demanda_total_q`; eran 1 886Q con los 98 días de la v5.9). Al arrancar el lun 14 en vez del vie 11 con el cierre fijo del 29-ene, la rotación lun-jue **no perdió ninguna sesión**: las 79 filas `banqueo1h` son las mismas, fecha por fecha, que en la v5.9 (solo baja 1 su D#); lo único que desapareció es el mini-sim del vie 11-sep (el D1 anterior), así que los viernes pasan de 19 a 18 y la cola larga de viernes rota una semana entera (el vie 29-ene cierra ahora con II-2 + I-10 en vez de V-6 + II-6). II-8 sigue con 4 sesiones (`extra.de = 4`, heredado de la v5.9; **A VERIFICAR (12-sep)** con Joseph si esa instancia debe recuperarse en la fase intensiva, igual que la de II-3 que se perdió en v5.8). Oferta en el pool: **434 ítems** (348 con clave oficial), de los cuales **254 siguen disponibles** tras pre-generar la semana 1, los 3 primeros mini-sims y el **pre-test de arranque** (inventario regenerado el 12-sep tras el pre-test: los 40Q del pre-test quedan consumidos, pero los 37 ítems de `banco_2026-09-14`/`banco_2026-09-15` que el pre-test no tomó vuelven al pool porque esas dos sesiones ya no se resuelven; neto +7). Cubre las **2 primeras semanas** de la rotación y los 3 primeros mini-sims; a partir de la semana 3 hay que reponer: por déficit absoluto los peores son **V-2 (179Q) · I-3 (165Q) · III-5 (90Q) · II-11 (85Q) · IV-6+IV-7 (84Q) · III-8 (82Q) · II-1 (79Q) · II-3 (75Q) · II-4 (75Q) · I-4 (74Q) · IV-1+IV-2 (72Q)**, y en cola larga **II-2 (29) · II-6 (28) · II-EMG (26) · I-OCC (25) · V-6 (25) · I-10 (24)** (cifras de `_meta.totales.deficit_codigos` del 12-sep post-pre-test, no estimadas). Disponibles hoy en los críticos: I-3 15 · II-3 15 · II-4 15 · II-5 20 · I-4 16 · IV-1+IV-2 18 · **V-2 1 · III-5 0** (V-2 y III-5 son los primeros que piden `set_<codigo>_2.json`: V-2 vuelve el mié 30-sep con clima_calidad y III-5 el jue 15-oct). Plan de reposición: (a) `set_<codigo>_2.json` con moldes 2024-2A→2026-1 + compendios (viernes tarde, gate §3-bis), (b) QX Banqueo/Theomed postests por ÁREA como complemento no filtrable por código (acceso 2027-I **A VERIFICAR (12-sep)**), (c) desde el 8-feb-2027 el 2026-II como cantera de espejos. Detalle por código: `_inventario_banco_por_codigo.json` (sus `_meta.descripcion`, `_meta.rotacion` y `_meta.totales` se leen del propio SQL: `dias_sembrados`, `d1`, `cierre`).

## 5) Cómo añadir un set nuevo (checklist)

1. Nombre `set_<codigo>_<n>.json`, `_meta` con `codigo`, `n`, `descripcion`, `generado`, `LISTA_NEGRA`.
2. Ids únicos con prefijo `N-<codigo sin signos>-<nnn>`; `formato` de 4 valores; `sub_eje` con las claves de `_encaps_ciclo_v3.js`.
3. `verificado_contra` = clave oficial (`CLAVE OFICIAL <proceso> Q<n>`) o cita textual del compendio/norma con número y año; lo no verificable se marca "no verificable contra clave oficial" y se dice explícitamente.
4. Nada del 2026-II (ni viñeta, ni molde, ni cifra-pregunta) hasta el 8-feb-2027.
5. `node DATA/_scripts/gen_encaps_minisim.js --inventario` para ver el nuevo stock; `--banco <fecha> --dry` para comprobar que el sub-eje del día queda cubierto.
