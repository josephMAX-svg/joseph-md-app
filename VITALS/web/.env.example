# ============================================================
# Pulso Movimiento — Web (Next.js). Copia a .env.local (NO subir a Git).
# ============================================================

# URL del microservicio de inteligencia (FastAPI). Server-to-server.
INTELLIGENCE_URL=http://127.0.0.1:8000

# (Producción) Supabase — ver supabase/ y CHECKLIST_HUMANO.md. En local NO se usan.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

NEXT_PUBLIC_APP_URL=http://localhost:3000
