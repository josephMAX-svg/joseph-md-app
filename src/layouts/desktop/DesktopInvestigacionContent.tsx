import React from 'react';
import ResearchHub from '../../components/study/ResearchHub';

/**
 * Desktop Research — Hub rediseñado (camino a Mayo Clinic): estructura ENCAPS
 * (prioridad/vueltas/links/deadline) + movimiento de Business + alternancia Research↔Derma.
 */
export default function DesktopInvestigacionContent() {
  return <ResearchHub variant="desktop" />;
}
